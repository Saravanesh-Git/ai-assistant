use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "local-assistant-development-only"
  limiter: false
  public_instance: false

search:
  formats:
    - html
    - json

